# FastAPI service dependencies.
fastapi
uvicorn[standard]
python-multipart
pydantic
openai>=1.0.0

# PyTorch is already present in the default Docker base image, but these
# entries make the requirement file usable outside Docker as well.
torch
torchvision
torchaudio

# Runtime transcription and MIDI-analysis dependencies.
backtrace==0.2.1
librosa==0.9.2
mir-eval==0.6
numpy==1.23.5
pretty-midi==0.2.9
pytorch-lightning==2.5.2
hydra-core==1.3.2
omegaconf==2.3.0
typing_extensions>=4.12.2
torchsummary==1.5.1
tqdm==4.64.1
immutabledict==2.2.3
pypianoroll==1.0.4
wandb
partitura
parangonar
h5py
tensorboard
plotly
matplotlib
symusic
audiomentations
peft>=0.13.0
transformers
pandas
scikit-learn
soundfile

# Optional GPU acceleration packages such as xformers and flash-attn are not
# installed by default because they are platform-specific. The model code treats
# them as optional and falls back to PyTorch attention when they are unavailable.
